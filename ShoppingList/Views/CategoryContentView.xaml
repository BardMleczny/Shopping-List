<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ShoppingList.Views"
             x:Class="ShoppingList.Views.CategoryContentView">
    <Frame Margin="5" Padding="10" BorderColor="Gray" CornerRadius="5">
        <VerticalStackLayout>
            <Label x:Name="CategoryLabel" Text="{Binding Name}" FontSize="20" FontAttributes="Bold" TextColor="Black" />
            <VerticalStackLayout x:Name="CategoryStackLayout" />
        </VerticalStackLayout>
    </Frame>
</ContentView>
